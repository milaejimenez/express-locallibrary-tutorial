<h1><%=title  %>: <%= genre.name  %></h1>

<% if ( genre_books.length ) { %>
    <p><strong>Delete the following books before attempting to delete this genre</strong></p>
    <div style='margin-left:20px;margin-top:20px'>
        <h4>Books</h4>
        <dl>
            <% for (const book of genre_books) { %>
                <dt>
                    <a href="<%= book.url %>"> <%= book.title %></a>
                </dt>
                <dd>
                    <%= book.summary %>
                </dd>
            <% } %>
        </dl>
    </div>
<% } else { %>
    <p>Do you really want to delete this genre?</p>
    <form method="POST" action="">
        <div class='form-group'>
            <input type="hidden" name="genderid" id="genderid" class="form-control"  value="<%= genre._id%>" required />
        </div>
        <button class="btn btn-primary" type="submit">Delete</button>
    </form>  
<% } %>